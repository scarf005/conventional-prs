minimum_prek_version = "0.3.0"
default_install_hook_types = ["commit-msg"]

[[repos]]
repo = "local"
hooks = [
  { id = "conventional-commit-title", name = "conventional commit title", language = "script", entry = "scripts/validate-commit-title.sh", stages = ["commit-msg"], always_run = true },
]
