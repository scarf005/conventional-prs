// @generated by scripts/build-action.mjs -- do not edit

// action/src/index.js
import { appendFile, readFile } from "node:fs/promises";
import { extname } from "node:path";
import { fileURLToPath } from "node:url";

// lib/rs_lib.internal.js
var wasm;
function __wbg_set_wasm(val) {
  wasm = val;
}
function getStringFromWasm0(ptr, len) {
  ptr = ptr >>> 0;
  return decodeText(ptr, len);
}
var cachedUint8ArrayMemory0 = null;
function getUint8ArrayMemory0() {
  if (cachedUint8ArrayMemory0 === null || cachedUint8ArrayMemory0.byteLength === 0) {
    cachedUint8ArrayMemory0 = new Uint8Array(wasm.memory.buffer);
  }
  return cachedUint8ArrayMemory0;
}
function isLikeNone(x) {
  return x === void 0 || x === null;
}
function passStringToWasm0(arg, malloc, realloc) {
  if (realloc === void 0) {
    const buf = cachedTextEncoder.encode(arg);
    const ptr2 = malloc(buf.length, 1) >>> 0;
    getUint8ArrayMemory0().subarray(ptr2, ptr2 + buf.length).set(buf);
    WASM_VECTOR_LEN = buf.length;
    return ptr2;
  }
  let len = arg.length;
  let ptr = malloc(len, 1) >>> 0;
  const mem = getUint8ArrayMemory0();
  let offset = 0;
  for (; offset < len; offset++) {
    const code = arg.charCodeAt(offset);
    if (code > 127) break;
    mem[ptr + offset] = code;
  }
  if (offset !== len) {
    if (offset !== 0) {
      arg = arg.slice(offset);
    }
    ptr = realloc(ptr, len, len = offset + arg.length * 3, 1) >>> 0;
    const view = getUint8ArrayMemory0().subarray(ptr + offset, ptr + len);
    const ret = cachedTextEncoder.encodeInto(arg, view);
    offset += ret.written;
    ptr = realloc(ptr, len, offset, 1) >>> 0;
  }
  WASM_VECTOR_LEN = offset;
  return ptr;
}
var cachedTextDecoder = new TextDecoder("utf-8", {
  ignoreBOM: true,
  fatal: true
});
cachedTextDecoder.decode();
var MAX_SAFARI_DECODE_BYTES = 2146435072;
var numBytesDecoded = 0;
function decodeText(ptr, len) {
  numBytesDecoded += len;
  if (numBytesDecoded >= MAX_SAFARI_DECODE_BYTES) {
    cachedTextDecoder = new TextDecoder("utf-8", {
      ignoreBOM: true,
      fatal: true
    });
    cachedTextDecoder.decode();
    numBytesDecoded = len;
  }
  return cachedTextDecoder.decode(
    getUint8ArrayMemory0().subarray(ptr, ptr + len)
  );
}
var cachedTextEncoder = new TextEncoder();
if (!("encodeInto" in cachedTextEncoder)) {
  cachedTextEncoder.encodeInto = function(arg, view) {
    const buf = cachedTextEncoder.encode(arg);
    view.set(buf);
    return {
      read: arg.length,
      written: buf.length
    };
  };
}
var WASM_VECTOR_LEN = 0;
function pretty_print_header(input) {
  let deferred2_0;
  let deferred2_1;
  try {
    const ptr0 = passStringToWasm0(
      input,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    const len0 = WASM_VECTOR_LEN;
    const ret = wasm.pretty_print_header(ptr0, len0);
    deferred2_0 = ret[0];
    deferred2_1 = ret[1];
    return getStringFromWasm0(ret[0], ret[1]);
  } finally {
    wasm.__wbindgen_free(deferred2_0, deferred2_1, 1);
  }
}
function pretty_print_header_with_config_auto(input, config_raw, format_hint) {
  let deferred4_0;
  let deferred4_1;
  try {
    const ptr0 = passStringToWasm0(
      input,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    const len0 = WASM_VECTOR_LEN;
    const ptr1 = passStringToWasm0(
      config_raw,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    const len1 = WASM_VECTOR_LEN;
    var ptr2 = isLikeNone(format_hint) ? 0 : passStringToWasm0(
      format_hint,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    var len2 = WASM_VECTOR_LEN;
    const ret = wasm.pretty_print_header_with_config_auto(
      ptr0,
      len0,
      ptr1,
      len1,
      ptr2,
      len2
    );
    deferred4_0 = ret[0];
    deferred4_1 = ret[1];
    return getStringFromWasm0(ret[0], ret[1]);
  } finally {
    wasm.__wbindgen_free(deferred4_0, deferred4_1, 1);
  }
}
function validate_header(input) {
  let deferred2_0;
  let deferred2_1;
  try {
    const ptr0 = passStringToWasm0(
      input,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    const len0 = WASM_VECTOR_LEN;
    const ret = wasm.validate_header(ptr0, len0);
    deferred2_0 = ret[0];
    deferred2_1 = ret[1];
    return getStringFromWasm0(ret[0], ret[1]);
  } finally {
    wasm.__wbindgen_free(deferred2_0, deferred2_1, 1);
  }
}
function validate_header_with_config_auto(input, config_raw, format_hint) {
  let deferred4_0;
  let deferred4_1;
  try {
    const ptr0 = passStringToWasm0(
      input,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    const len0 = WASM_VECTOR_LEN;
    const ptr1 = passStringToWasm0(
      config_raw,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    const len1 = WASM_VECTOR_LEN;
    var ptr2 = isLikeNone(format_hint) ? 0 : passStringToWasm0(
      format_hint,
      wasm.__wbindgen_malloc,
      wasm.__wbindgen_realloc
    );
    var len2 = WASM_VECTOR_LEN;
    const ret = wasm.validate_header_with_config_auto(
      ptr0,
      len0,
      ptr1,
      len1,
      ptr2,
      len2
    );
    deferred4_0 = ret[0];
    deferred4_1 = ret[1];
    return getStringFromWasm0(ret[0], ret[1]);
  } finally {
    wasm.__wbindgen_free(deferred4_0, deferred4_1, 1);
  }
}
function __wbindgen_init_externref_table() {
  const table = wasm.__wbindgen_externrefs;
  const offset = table.grow(4);
  table.set(0, void 0);
  table.set(offset + 0, void 0);
  table.set(offset + 1, null);
  table.set(offset + 2, true);
  table.set(offset + 3, false);
}

// action/src/index.js
import process from "node:process";
var COMMENT_MARKER = "<!-- conventional-prs-validation -->";
var CONFIG_PATHS = [
  ".github/semantic.yml",
  ".github/semantic.yaml",
  ".github/semantic.json",
  ".github/semantic.jsonc",
  ".github/semantic.toml"
];
var wasmReady;
var readActionInput = (name, env = process.env) => {
  const normalized = name.replace(/ /gu, "_").toUpperCase();
  const key = `INPUT_${normalized}`;
  return env[key];
};
var parseBooleanInput = (value, defaultValue = false) => {
  if (value === void 0 || value === null || value.trim().length === 0) {
    return defaultValue;
  }
  const normalized = value.trim().toLowerCase();
  if (["1", "true", "yes", "on"].includes(normalized)) {
    return true;
  }
  if (["0", "false", "no", "off"].includes(normalized)) {
    return false;
  }
  return defaultValue;
};
var inferConfigFormat = (path) => {
  switch (extname(path).toLowerCase()) {
    case ".yml":
      return "yml";
    case ".yaml":
      return "yaml";
    case ".json":
      return "json";
    case ".jsonc":
      return "jsonc";
    case ".toml":
      return "toml";
    default:
      return void 0;
  }
};
var readPullRequest = (eventPayload) => {
  const pullRequest = eventPayload?.pull_request;
  const number = pullRequest?.number;
  const title = pullRequest?.title;
  if (!Number.isInteger(number)) {
    throw new Error("Invalid pull request number in event payload");
  }
  if (typeof title !== "string" || title.length === 0) {
    throw new Error("Missing pull request title in event payload");
  }
  return { number, title };
};
var buildFailureComment = (report) => {
  return [
    COMMENT_MARKER,
    "## PR Title Validation Failed",
    "",
    "```",
    report,
    "```",
    "",
    "Your PR title must follow Conventional Commits."
  ].join("\n");
};
var encodePath = (path) => path.split("/").map(encodeURIComponent).join("/");
var appendMultilineOutput = async (name, value) => {
  const outputFile = process.env.GITHUB_OUTPUT;
  if (!outputFile) {
    return;
  }
  await appendFile(
    outputFile,
    `${name}<<__CONVENTIONAL_PRS__
${value}
__CONVENTIONAL_PRS__
`
  );
};
var writeOutputs = async ({ valid, configPath, report }) => {
  await appendMultilineOutput("valid", valid ? "true" : "false");
  await appendMultilineOutput("config-path", configPath);
  await appendMultilineOutput("report", report);
};
var appendSummary = async (content) => {
  const summaryFile = process.env.GITHUB_STEP_SUMMARY;
  if (!summaryFile) {
    return;
  }
  await appendFile(summaryFile, `${content}
`);
};
var githubRequest = (url, options = {}) => {
  const headers = new Headers(options.headers ?? {});
  if (options.token) {
    headers.set("Authorization", `Bearer ${options.token}`);
  }
  return fetch(url, {
    method: options.method ?? "GET",
    headers,
    body: options.body
  });
};
var listValidationComments = async ({ repository, pullRequestNumber, token }) => {
  if (!token) {
    return [];
  }
  const url = `https://api.github.com/repos/${repository}/issues/${pullRequestNumber}/comments?per_page=100`;
  const response = await githubRequest(url, {
    token,
    headers: {
      Accept: "application/vnd.github+json"
    }
  });
  if (!response.ok) {
    throw new Error(
      `Failed to list PR comments: ${response.status} ${response.statusText}`
    );
  }
  const comments = await response.json();
  if (!Array.isArray(comments)) {
    throw new Error("Unexpected comment payload from GitHub API");
  }
  return comments.filter(
    (comment) => typeof comment?.body === "string" && comment.body.includes(COMMENT_MARKER)
  );
};
var upsertFailureComment = async ({ repository, pullRequestNumber, token, report }) => {
  if (!token) {
    return;
  }
  const body = buildFailureComment(report);
  const comments = await listValidationComments({
    repository,
    pullRequestNumber,
    token
  });
  const existing = comments[0];
  if (existing) {
    const updateUrl = `https://api.github.com/repos/${repository}/issues/comments/${existing.id}`;
    const updateResponse = await githubRequest(updateUrl, {
      method: "PATCH",
      token,
      headers: {
        Accept: "application/vnd.github+json",
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ body })
    });
    if (!updateResponse.ok) {
      throw new Error(
        `Failed to update PR comment: ${updateResponse.status} ${updateResponse.statusText}`
      );
    }
    return;
  }
  const createUrl = `https://api.github.com/repos/${repository}/issues/${pullRequestNumber}/comments`;
  const createResponse = await githubRequest(createUrl, {
    method: "POST",
    token,
    headers: {
      Accept: "application/vnd.github+json",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({ body })
  });
  if (!createResponse.ok) {
    throw new Error(
      `Failed to create PR comment: ${createResponse.status} ${createResponse.statusText}`
    );
  }
};
var deleteValidationComments = async ({ repository, pullRequestNumber, token }) => {
  if (!token) {
    return;
  }
  const comments = await listValidationComments({
    repository,
    pullRequestNumber,
    token
  });
  for (const comment of comments) {
    const deleteUrl = `https://api.github.com/repos/${repository}/issues/comments/${comment.id}`;
    const response = await githubRequest(deleteUrl, {
      method: "DELETE",
      token,
      headers: {
        Accept: "application/vnd.github+json"
      }
    });
    if (!response.ok) {
      throw new Error(
        `Failed to delete PR comment: ${response.status} ${response.statusText}`
      );
    }
  }
};
var loadRepoConfig = async ({ repository, token }) => {
  for (const path of CONFIG_PATHS) {
    const url = `https://api.github.com/repos/${repository}/contents/${encodePath(path)}`;
    const response = await githubRequest(url, {
      token,
      headers: {
        Accept: "application/vnd.github.raw+json"
      }
    });
    if (response.status === 404) {
      continue;
    }
    if (!response.ok) {
      throw new Error(
        `Failed to fetch ${path}: ${response.status} ${response.statusText}`
      );
    }
    const content = await response.text();
    return {
      path,
      format: inferConfigFormat(path),
      content
    };
  }
  return null;
};
var wasmUrls = [
  new URL("./rs_lib.wasm", import.meta.url),
  new URL("../../lib/rs_lib.wasm", import.meta.url)
];
var loadWasmBytes = async () => {
  for (const url of wasmUrls) {
    try {
      return await readFile(url);
    } catch (error) {
      if (error && typeof error === "object" && "code" in error) {
        const code = error.code;
        if (code === "ENOENT" || code === "ENOTDIR") {
          continue;
        }
      }
      throw error;
    }
  }
  throw new Error("Unable to locate bundled rs_lib.wasm");
};
var initWasm = async () => {
  const bytes = await loadWasmBytes();
  const { instance } = await WebAssembly.instantiate(bytes, {
    "./rs_lib.internal.js": {
      __wbindgen_init_externref_table
    }
  });
  const wasmExports = instance.exports;
  __wbg_set_wasm(wasmExports);
  if (typeof wasmExports.__wbindgen_start === "function") {
    wasmExports.__wbindgen_start();
  } else {
    __wbindgen_init_externref_table();
  }
};
var ensureWasm = async () => {
  if (!wasmReady) {
    wasmReady = initWasm();
  }
  await wasmReady;
};
var parseValidationResult = (raw) => JSON.parse(raw);
var validateTitle = async ({ title, configFile }) => {
  await ensureWasm();
  const validation = configFile === null ? parseValidationResult(validate_header(title)) : parseValidationResult(
    validate_header_with_config_auto(
      title,
      configFile.content,
      configFile.format
    )
  );
  if (validation.ok) {
    return {
      valid: true,
      report: ""
    };
  }
  const report = configFile === null ? pretty_print_header(title) : pretty_print_header_with_config_auto(
    title,
    configFile.content,
    configFile.format
  );
  return {
    valid: false,
    report
  };
};
var run = async () => {
  const eventName = process.env.GITHUB_EVENT_NAME;
  if (eventName !== "pull_request" && eventName !== "pull_request_target") {
    console.log(
      "This action only runs on pull_request or pull_request_target events"
    );
    await writeOutputs({ valid: true, configPath: "", report: "" });
    return;
  }
  if (!parseBooleanInput(
    readActionInput("validate-pr-title") ?? process.env.INPUT_VALIDATE_PR_TITLE,
    true
  )) {
    console.log("PR title validation is disabled");
    await writeOutputs({ valid: true, configPath: "", report: "" });
    return;
  }
  const eventPath = process.env.GITHUB_EVENT_PATH;
  if (!eventPath) {
    throw new Error("GITHUB_EVENT_PATH is required");
  }
  const repository = process.env.GITHUB_REPOSITORY;
  if (!repository) {
    throw new Error("GITHUB_REPOSITORY is required");
  }
  const token = readActionInput("github-token") ?? process.env.GITHUB_TOKEN ?? "";
  const eventPayload = JSON.parse(await readFile(eventPath, "utf8"));
  const pullRequest = readPullRequest(eventPayload);
  const configFile = await loadRepoConfig({ repository, token });
  if (configFile) {
    console.log(`Using config: ${configFile.path}`);
  } else {
    console.log("No repository config found, using defaults");
  }
  console.log(`Validating PR #${pullRequest.number}: ${pullRequest.title}`);
  const validation = await validateTitle({
    title: pullRequest.title,
    configFile
  });
  await writeOutputs({
    valid: validation.valid,
    configPath: configFile?.path ?? "",
    report: validation.report
  });
  if (validation.valid) {
    console.log("PR title is valid");
    await deleteValidationComments({
      repository,
      pullRequestNumber: pullRequest.number,
      token
    });
    return;
  }
  const report = validation.report.trimEnd();
  console.error(report);
  await appendSummary(["```", report, "```"].join("\n"));
  await upsertFailureComment({
    repository,
    pullRequestNumber: pullRequest.number,
    token,
    report
  });
  process.exitCode = 1;
};
var isMainModule = process.argv[1] === fileURLToPath(import.meta.url);
if (isMainModule) {
  run().catch(async (error) => {
    const message = error instanceof Error ? error.message : String(error);
    console.error(message);
    await writeOutputs({ valid: false, configPath: "", report: message });
    process.exitCode = 1;
  });
}
export {
  buildFailureComment,
  inferConfigFormat,
  parseBooleanInput,
  readActionInput,
  readPullRequest,
  run
};
